/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['github.com', 'images.unsplash.com', 'tailwindui.com'],
  },
};

module.exports = nextConfig;
